# Host identity so files match your macOS user
HOST_UID=1000
HOST_GID=1000
USERNAME=dev
# Optional: where to mount host dotfiles if you uncomment those lines
CONTAINER_HOME=/home/dev

# Set default platform for builds; for Apple Silicon use arm64 by default
DOCKER_DEFAULT_PLATFORM=linux/arm64